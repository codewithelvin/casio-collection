# W-650 — seeded 2026-08-16 (M1c, the sourcing spike).
#
# One model, read off one page in the Digital Watch Library. That is a
# community source and is labelled as one: casio.com answers 403 to anything
# that is not a person, so no official page was available to prefer.
#
# Movement is absent because the page does not state one, and display is absent
# unless the page describes the dial. The page shows a "light" icon with no
# type, and the vocabulary has five specific light values and no generic one,
# so no light feature is claimed. Absent means nobody has found it (D27).

series:
  id: w-650
  name: W-650
  line: vintage
  aka: ['Multi Alarm']

models:
  - id: w-650
    ref: W-650
    source: { url: 'http://www.digital-watch.com/DWL/1work/casio-w-650', kind: community }
    year: 1983
    module: '181'
    case: { material: stainless steel }
    water_resistance_m: 100
    features: [multi-alarm, stopwatch, dual-time, countdown-timer]
    # `image: null` REFUSED ON SIGHT, 2026-08-25, not for want of a candidate.
    # The page names a photograph and it downloads at 800 px, which is the
    # largest of any Digital Watch Library file taken for this line — and it is
    # a picture of the **caseback**, lying on a carpet, with the photographer's
    # thumb in the bottom-right corner. It is excellent evidence that the
    # reference is real: the engraving reads CASIO W-650 JAPAN. It is not a
    # picture of the watch anybody is looking for.
    #
    # This is the case the skill's "a lifestyle photo is not usable" rule is
    # about, and the alt text agreed it was W-650 — so the alt check passed and
    # looking at the file is what caught it. The typographic card is a primary
    # state (D29) and is the better answer here.
    image: null
    discontinued: true
