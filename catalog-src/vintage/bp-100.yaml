# BP-100 — added 2026-08-25 by /finish-casio.
#
# NEITHER MODERN ROSTER KNOWS THIS WATCH. Not in Casio's current sitemap, not in
# the archive's per-segment CDX index — the survey returned no references at all
# from either roster. The blood-pressure watch never had a PRODUCT page on the
# casio.com the archive kept, which is the only thing those rosters ask about.
# Casio does publish it on a third surface, its 50th-anniversary Heritage
# timeline, and that page is what the year below now comes from.
#
# THE VOCABULARY CANNOT SAY WHAT THIS WATCH IS FOR, and that is the one thing
# worth knowing about it. The case prints BLOOD PRESSURE MONITOR, the strap
# prints PLS EKG, there is a SENSOR pad under the display and the dial is
# labelled SYSTOLIC and DIASTOLIC. The page's own function icon says "I take
# your pulse". `src/catalog/vocabulary.ts` had altimeter, barometer, compass,
# thermometer, step-counter, tide-graph, moon-data and sunrise-sunset under
# Sensors, and nothing for a pulse, an ECG or a blood pressure.
#
# HALF OF THAT WAS ANSWERED ON 2026-08-27. The client closed the decision this
# note asked for and the Sensors group gained `pulse-sensor`, so the entry below
# now carries it, on a headcount of four: this file, JP-100W and JP-200W each
# carry DWL's pulse icon, and CHR-100's case prints HEART RATE. CPA-100 is NOT
# one of them — it was named beside this entry as the other watch silent about
# its own purpose, and its gap is prayer times, still open.
#
# WHAT IS STILL UNSAYABLE HERE is the rest of what makes this watch odd: there is
# no value for a BLOOD PRESSURE MONITOR and none for an EKG, so the case prints
# two claims the catalogue cannot repeat. Reported, not invented.
#
# HOW THE PAGE WAS READ. DWL pairs a label div with a value div by id; the
# reader was calibrated against F-87W, already reviewed here, and reproduced its
# year, module and case exactly first. The case is the `> CASING` row (Resin),
# not the `> BRACELET NO.` row ([Resin]) that sits next to it.
#
# The photograph is the strongest reference proof taken in this line: the strap
# moulds BP-100 in raised letters. That is the "prefer a dial that prints the
# model name" test passing outright rather than an alt tag being trusted.
#
# THE YEAR MOVED 1990 -> 1992 ON 2026-08-27, and it is an overwrite rather than
# a fill, so it is written down. DWL, the page in `source`, says 1990. Casio's
# own 50th-anniversary Heritage timeline dates it 1992 and describes this exact
# watch — "No wristwatch had ever incorporated a blood pressure monitor before …
# it also stored up to 30 datapoints in memory, including pulse". An official
# source outranks a community one, so `year_source` now names the page that
# states it, which is the same call AL-180 and MAP-100 already carry.
#
# ABSENT AND WHY:
#   * `display` — the page never describes the dial. Its title slot reads
#     "Digital", but that slot holds "Casiotron" and "Calculator" on the sibling
#     pages read in this same run, so it is a CATEGORY and not a display type.
#   * `movement` — the page states none.
#   * the light icon — the vocabulary has five specific light values and no
#     generic one, so no light feature is claimed. Same as A168 and AL-180.
#   * `discontinued` — never written by hand; availability.ts measures it (D59).

series:
  id: bp-100
  name: BP-100
  line: vintage

models:
  - id: bp-100
    ref: BP-100
    source: { url: 'http://www.digital-watch.com/DWL/1work/casio_bp-100', kind: community }
    year: 1992
    year_source: 'https://web.archive.org/web/20240722022557id_/https://www.casio.com/intl/watches/50th/Heritage/1990s/'
    module: '900'
    case: { material: resin }
    water_resistance_m: 50
    # `pulse-sensor` added 2026-08-27 — see the header. It comes off the page
    # already named in `source`: DWL's function icon reads "I take your pulse".
    # The BLOOD PRESSURE MONITOR and PLS EKG the case prints are STILL unsayable,
    # so this entry is only partly un-silenced.
    features: [alarm, stopwatch, pulse-sensor]
    image: bp-100
    image_credit:
      author: The Digital Watch Library
      licence: rights-reserved
      url: 'http://www.digital-watch.com/DWL/1work/casio_bp-100'
    discontinued: true
