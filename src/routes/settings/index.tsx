import { Placeholder } from '../../ui/Placeholder'

/** M8: handle, sharing toggle, theme, export, account deletion (§3.7). */
export default function SettingsRoute() {
  return <Placeholder titleKey="route.settings.title" />
}
